#include<iostream>
using namespace std;
int main()
{
 int arr[5] = {1, 2, 3, 4, 5}; //array treated as a constent pointer 
 int a = 25 ; 
//  arr = &a ; // not possible , because arr is an array and a is a pointer
    return 0;
}